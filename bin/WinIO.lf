extern "GetStdHandle";
extern "WriteConsoleA";

int len(string s)
{
    while(true)
    {
        
    }
}

void Print(string str)
{
    int* handle = GetStdHandle(-11);
    if(handle != 0)
    {
        int* a;
        WriteConsoleA(0,a,len(str),str,handle);
    }
}